//import { useParams } from "react-router-dom";
import ItemDetailContainer from "../components/ItemDetailContainer/ItemDetailContainer";
const Detail = ()=>{
    //const idItem = useParams(); //Aca no anda, tampoco si uso UseParam en App directo...
    //console.log("useParam : ", idItem)    nroItem = {idItem}
    return(
        
        <ItemDetailContainer />

    )
}

export default Detail;